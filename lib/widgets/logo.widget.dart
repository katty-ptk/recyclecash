import 'package:flutter/material.dart';

class RecycleCashLogo extends StatelessWidget {
  const RecycleCashLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('RecycleCash');
  }
}
